# Normalize line endings to LF in the repo (we author on Windows).
* text=auto eol=lf

# Binary assets (don't normalize / diff as text)
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.mp3 binary
*.ogg binary
*.wav binary
*.zip binary
