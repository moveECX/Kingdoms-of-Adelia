{
  "name": "@aldermark/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Svelte 5 + Vite client: city grid, world map (Canvas), data-dense dark UI.",
  "scripts": {
    "dev": "vite",
    "build": "svelte-check --tsconfig ./tsconfig.json && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@aldermark/shared": "*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.15.0",
    "svelte-check": "^4.1.0",
    "vite": "^6.0.0"
  }
}
